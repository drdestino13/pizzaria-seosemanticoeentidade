---
interface Props {
  slug: string;
}
const { slug } = Astro.props;
const outro = `https://pizzaria-seotradicional.vercel.app/${slug}/`;
---
<p><a href={outro} rel="nofollow noopener" target="_blank">Veja o exemplo ERRADO deste conceito no site irmão →</a></p>
